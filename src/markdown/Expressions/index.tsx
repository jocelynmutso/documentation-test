import indexLocation from './_index.md';


const Expressions = {
  name: 'Expressions', path: indexLocation,
  subs: [
    //{ name: 'Dialob Expression Language (DEL)', path: main },
  ]
}

export default Expressions;